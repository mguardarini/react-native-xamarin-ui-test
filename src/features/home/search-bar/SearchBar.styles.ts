import {
    StyleSheet
  } from 'react-native';

import {white,gray,gray74} from '../../../styles/colors';

export default StyleSheet.create({
  textInputStyle: {
    color:gray74,
    backgroundColor:white,
    paddingLeft:15,
    height: 50,
    borderColor: gray,
    borderWidth: 1, 
    borderRadius:10 
  }
});